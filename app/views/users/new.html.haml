%h2 Register as a new user

= bootstrap_form_for @user do |f|
  = f.text_field :name, placeholder: "Your Name"
  = f.email_field :email, placeholder: "youremail@domain.com"
  = f.password_field :password, placeholder: "Your Password"
  = f.submit "Register"